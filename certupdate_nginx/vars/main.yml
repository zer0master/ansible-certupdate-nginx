---
#NOTE: set certs_local/remote_* values from the command line or in your playbook with the role; cert_info reflects
# nxinx and LetsEncrypt conventions
#
#certs_local_prefix:
#certs_target_folder: /etc/ssl/nginx/certs
cert_info:
  - filename: fullchain.pem
    mode: '0640'
  - filename: privkey.pem
    mode: '0400'

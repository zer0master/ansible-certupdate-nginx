---
#NOTE: set to reflect your choice of role name/; if something other than nginx is used, override it too
certs_local_folder: roles/revproxy/files/certs
certs_target_folder: /etc/ssl/nginx/certs
cert_info:
  - filename: fullchain.pem
    mode: '0640'
  - filename: privkey.pem
    mode: '0400'
